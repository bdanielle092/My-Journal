const createjournalEntry = (date, concepts, entry, mood) => {
    newJournalEntry = {
        date: date,
        concepts: concepts,
        entry: entry,
        mood: mood
    }
    return journalEntry    
}

export default createjournalEntry

